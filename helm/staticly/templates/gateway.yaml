apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: staticly-gateway
  namespace: telekemp
spec:
  selector:
    istio: ingressgateway   # must match your ingress deployment label
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
