apiVersion: v1
kind: Service
metadata:
  name: {{ include "staticly.fullname" . }}
  labels:
    {{- include "staticly.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      #nodePort: {{ .Values.service.nodePort }}
  selector:
    {{- include "staticly.selectorLabels" . | nindent 4 }}
