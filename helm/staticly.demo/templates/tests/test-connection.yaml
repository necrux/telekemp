apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "staticly.fullname" . }}-test-connection"
  labels:
    {{- include "staticly.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "staticly.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
